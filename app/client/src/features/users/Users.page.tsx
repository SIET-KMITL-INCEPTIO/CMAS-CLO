export default function Users() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-medium">จัดการผู้ใช้งาน</h1>
      {/* TODO: 5.1.1 — list users, toggle role/active via useQuery + apiClient */}
    </div>
  )
}
