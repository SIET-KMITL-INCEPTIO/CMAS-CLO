# superseded/

Older copies of diagrams in the parent folder. They are kept, not deleted, so a
version shown in a past meeting still opens, but nobody should open one of these
by mistake for a current presentation.

| File | Current version | Why it is here |
|---|---|---|
| `UML-INDEX-Q_2026-09-14.drawio` | `../UML-INDEX-Q.drawio` | Was `UML-INDEX-Q copy.drawio`, a hand-made copy from 2026-09-14. `../UML-INDEX-Q.drawio` is regenerated by `scripts/build-index-q-uml-drawio.py` (last 2026-09-18), and the two files differ. **Not yet checked** whether the copy holds hand edits worth carrying forward. Delete it once someone has compared them. |
| `CMAS-Architecture.drawio` | none (no copy in the parent folder) | Software architecture of the code at commit `ff6ed7c` (2026-09-24), 8 pages: stack-and-flow overview in the style of the team slide (numbered steps ①–⑩, library icons loaded from the jsDelivr CDN so they need a network connection to show), 3-tier overview, client data flow, server request pipeline, Google-login sequence, admin-approve sequence, data layer and trust boundaries, and a tool-level sequence (Fastify / jose / Zod / Prisma / Postgres) for `GET /users`. Placed here on request rather than in `../`. Not generated by a script under `scripts/`, so `check-diagrams.py` does not regenerate or compare it. Redraw it once the `courses` / `clos` / `excel` / `dashboard` modules exist. |
| `CMAS-FE-BE-DB.drawio` | none | One page, Frontend → Backend → Database, walking one request (ADMIN approves a user, `PATCH /users/:id/approve`) with steps ①–⑩ and the tool that handles each step (React, TanStack Query, Fastify, jose, Zod, Prisma, Postgres). Kept as its own file so it is not overwritten while `CMAS-Architecture.drawio` is open in draw.io. Same caveats as that file: not script-generated in `scripts/`, icons load from the jsDelivr CDN. |
| `CMAS-Flowcharts.drawio` | none | One flowchart per use case (49 pages, numbered as in `../UML-INDEX-Q.drawio`) plus a contents page. The steps, conditions and error messages are read from the prototype `docs/pages/index-q.html` (e.g. `FORMS.*.validate`, `validateWorkbook`, `save-course`), not from `app/server`, which only implements auth and users so far. Generated from a script that is not yet in `scripts/`, so `check-diagrams.py` does not regenerate it. |
