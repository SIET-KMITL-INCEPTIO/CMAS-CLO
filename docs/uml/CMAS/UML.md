# UML — CMAS

**Layer 0 · ภาพรวมทั้งระบบ** · ปรับล่าสุด 2569-09-13

<!-- STATUS:AUTHORITATIVE
     บรรทัดที่ขึ้นต้นด้วย STATUS: ถูกอ่านโดย scripts/check-diagrams.py
     ห้ามลบ ถ้าจะเปลี่ยนสถานะของไฟล์ให้แก้ที่นี่ที่เดียว
STATUS:SUPERSEDED UML-Layer2.drawio เลข use case เป็นฉบับร่างแรก ใช้ index-q แทน
-->

## ไฟล์ไหนเป็นฉบับจริงของอะไร

อ่านตารางนี้ก่อนหยิบไฟล์ไปขึ้นจอ · ทุกไฟล์ในโฟลเดอร์นี้ไม่ได้มีสถานะเท่ากัน

| ไฟล์ | สถานะ | ใช้ตอนไหน |
|---|---|---|
| **UML.md** (ไฟล์นี้) | ✅ ฉบับจริงของ **เลข use case** | อ้างเลข 1.1–7.5 ทุกครั้ง |
| `index-q/index-q-usecase.drawio` | ✅ ฉบับจริงของ **รายละเอียดและสิทธิ์** · สร้างจากโค้ด 6 หน้า | เจาะรายแพ็กเกจ · UML 2.5 เต็มรูป |
| `index-q/UML-INDEX-Q.drawio` | ✅ ข้อมูลชุดเดียวกัน หน้าเดียว สัญกรณ์เดียวกับ Layer2 | ภาพรวมบนสไลด์เดียว |
| `Struture.drawio` | ✅ โครงสร้างสิทธิ์ 3 หน้า | อธิบายบทบาทและ capability |
| `UML-Layer1.drawio` | ⚠ ร่างแรก | ประกอบเท่านั้น |
| `UML-Layer2.drawio` | ⛔ **ร่างแรก · เลขไม่ตรงกับไฟล์นี้** | **อย่าฉายคู่กับไฟล์อื่น** |

> **ทำไม Layer2 ถึงห้ามฉายคู่** — `scripts/check-diagrams.py` วัดแล้ว: เลข 10 ตัว
> ในนั้นหมายถึงคนละ use case กับไฟล์นี้ (เช่น 1.4 ในไฟล์นี้คือ *กำหนดบทบาทและสิทธิ์*
> แต่ใน Layer2 คือ *reset รหัสผ่าน*) · มีเลขซ้ำ 1 จุด และ use case ที่ไม่มีเลข 13 ข้อ
> ถ้าขึ้นจอเรียงกัน คนดูจะเห็นเลขเดียวกันหมายถึงสองเรื่องภายในสไลด์ติดกัน
>
> **ไม่ได้แก้ Layer2 เพราะมันแยกย่อยคนละแบบ** — Layer2 แยก *แก้ไข CLO* กับ *ลบ CLO*
> เป็นสองข้อ ส่วนไฟล์นี้รวมเป็น *3.2 แก้ไข / ลบ CLO* · การไล่เปลี่ยนเลขให้ตรงคือ
> การรื้อการแยกย่อยของคนอื่น ไม่ใช่การแก้เลข

## ข้อควรรู้ก่อนนำเสนอ

* **actor ในภาพนี้มี 2 ตัว แต่ระบบจริงมี 4** — `CourseRole` ถูกบังคับใช้จริงแล้ว
  (ASM-03a) เป็น ผู้ช่วยสอน ◁ ผู้สอนร่วม ◁ ผู้ประสานงานรายวิชา และ ผู้ดูแลระบบ
  ภาพสามบทบาทอยู่ใน `Struture.drawio` หน้า 2 และ `index-q/UML-INDEX-Q.drawio`
* **ผู้ดูแลระบบไม่ทำงานในรายวิชา** (ASM-03b) — สร้าง ลบ และผูกผู้สอนเข้ากับรายวิชาเท่านั้น
* **1.3 คือ *ระงับ* ไม่ใช่ *ลบ*** — FR-06 และ `onDelete: Restrict` ห้ามลบบัญชีที่ผูกรายวิชา
  เดิมไฟล์นี้เขียนว่า "ระงับ / ลบ" ซึ่งสัญญาสิ่งที่สคีมาปฏิเสธ · แก้แล้ว 2569-09-13

---

```mermaid
flowchart LR
    ADMIN(["ผู้ดูแลระบบ"])
    TEACHER(["ผู้สอน"])

    subgraph SYS["ระบบ CMAS"]
        direction TB

        subgraph P1["&laquo;package&raquo; 1. จัดการผู้ใช้งาน"]
            direction TB
            U11(["1.1 เพิ่มบัญชีผู้ใช้"])
            U12(["1.2 แก้ไขบัญชีผู้ใช้"])
            U13(["1.3 ระงับการใช้งานบัญชี"])
            U14(["1.4 กำหนดบทบาทและสิทธิ์"])
            U15(["1.5 รีเซ็ตรหัสผ่าน"])
            U16(["1.6 ตรวจสอบประวัติการใช้งาน"])
        end

        subgraph P2["&laquo;package&raquo; 2. กรอกข้อมูลรายวิชา"]
            direction TB
            U21(["2.1 สร้างรายวิชา"])
            U22(["2.2 แก้ไขข้อมูลรายวิชา"])
            U23(["2.3 ลบรายวิชา"])
            U24(["2.4 กำหนดผู้สอนประจำรายวิชา"])
            U25(["2.5 จัดการ Sec / ภาคการศึกษา"])
        end

        subgraph P3["&laquo;package&raquo; 3. กำหนด CLOs ของรายวิชา"]
            direction TB
            U31(["3.1 เพิ่ม CLO"])
            U32(["3.2 แก้ไข / ลบ CLO"])
            U33(["3.3 กำหนดน้ำหนักและเกณฑ์ผ่านของ CLO"])
            U34(["3.4 เชื่อมโยง CLO กับ PLO"])
        end

        subgraph P4["&laquo;package&raquo; 4. เพิ่มวัตถุประสงค์เชิงพฤติกรรม"]
            direction TB
            U41(["4.1 เพิ่มวัตถุประสงค์เชิงพฤติกรรม"])
            U42(["4.2 เพิ่มวิธีการประเมิน"])
            U43(["4.3 เพิ่มเกณฑ์การประเมิน"])
            U44(["4.4 จับคู่วัตถุประสงค์กับ CLO"])
            U45(["4.5 แก้ไข / ลบวัตถุประสงค์"])
        end

        subgraph P5["&laquo;package&raquo; 5. วิเคราะห์และสรุปข้อมูล CLOs"]
            direction TB
            U51(["5.1 วิเคราะห์และสรุปข้อมูล CLOs"])
            U52(["5.2 คำนวณคะแนนตาม CLO"])
            U53(["5.3 แสดงผลสรุประดับรายวิชา"])
            U54(["5.4 ติดตามผู้เรียนรายบุคคล"])
            U55(["5.5 ส่งออกรายงาน"])
        end

        subgraph P6["&laquo;package&raquo; 6. นำเข้า-ส่งออกคะแนนกิจกรรม"]
            direction TB
            U61(["6.1 นำเข้าคะแนนกิจกรรม"])
            U62(["6.2 ดาวน์โหลดเทมเพลตคะแนน"])
            U63(["6.3 ตรวจสอบความถูกต้องของข้อมูล"])
            U64(["6.4 ส่งออกคะแนนกิจกรรม"])
            U65(["6.5 แสดงรายงานข้อผิดพลาดการนำเข้า"])
        end

        subgraph P7["&laquo;package&raquo; 7. นำเข้า-ส่งออกรายชื่อนักศึกษา"]
            direction TB
            U71(["7.1 นำเข้ารายชื่อนักศึกษา"])
            U72(["7.2 ดาวน์โหลดเทมเพลตรายชื่อ"])
            U73(["7.3 ตรวจสอบรูปแบบและรายการซ้ำ"])
            U74(["7.4 ส่งออกรายชื่อนักศึกษา"])
            U75(["7.5 แสดงรายงานข้อผิดพลาดการนำเข้า"])
        end
    end

    ADMIN --- U11
    ADMIN --- U12
    ADMIN --- U13
    ADMIN --- U14
    ADMIN --- U15
    ADMIN --- U16

    TEACHER --- U21
    TEACHER --- U22
    TEACHER --- U23
    TEACHER --- U24
    TEACHER --- U25
    TEACHER --- U31
    TEACHER --- U32
    TEACHER --- U34
    TEACHER --- U41
    TEACHER --- U45
    TEACHER --- U51
    TEACHER --- U61
    TEACHER --- U62
    TEACHER --- U64
    TEACHER --- U71
    TEACHER --- U72
    TEACHER --- U74

    U11 -.->|"include"| U14
    U21 -.->|"include"| U24
    U31 -.->|"include"| U33
    U41 -.->|"include"| U44
    U41 -.->|"include"| U42
    U42 -.->|"include"| U43
    U51 -.->|"include"| U52
    U51 -.->|"include"| U53
    U61 -.->|"include"| U63
    U71 -.->|"include"| U73

    U34 -.->|"extend ep: หลังบันทึก CLO"| U31
    U54 -.->|"extend ep: เลือกผู้เรียนรายบุคคล"| U51
    U55 -.->|"extend ep: หลังแสดงผลสรุป"| U51
    U65 -.->|"extend ep: พบข้อมูลไม่ถูกต้อง"| U61
    U75 -.->|"extend ep: พบข้อมูลไม่ถูกต้อง"| U71

    classDef actor fill:#1E233C,stroke:#878CB4,color:#C8CDEB
    classDef uc fill:#0A0F19,stroke:#465078,color:#C8CDEB
    class ADMIN,TEACHER actor
    class U11,U12,U13,U14,U15,U16,U21,U22,U23,U24,U25,U31,U32,U33,U34,U41,U42,U43,U44,U45,U51,U52,U53,U54,U55,U61,U62,U63,U64,U65,U71,U72,U73,U74,U75 uc

```