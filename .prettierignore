# Dependencies and build output
node_modules
dist
build
*.tsbuildinfo

# The whole docs tree is excluded on purpose.
#
#   docs/html/       generated by docs/_generator/build.mjs — never hand-edited
#   docs/markdown/   hand-maintained Thai-language prose with manually aligned
#                    tables and mermaid blocks. Prettier realigns every table,
#                    which produces a large diff and no benefit, and its
#                    handling of CJK/Thai width in tables is not reliable.
#   docs/_generator/ a separate mini-project with its own package.json
#
# Format code, not documents.
docs/

# Lockfiles
package-lock.json

# Prisma generates its own formatting via `prisma format`
database/migrations

# Binary-ish / data dumps
*.sql
*.pdf
*.mwb
*.bak
