# Fail fast if someone runs the project on Node < 20 rather than letting it
# break at runtime with a confusing error. Matches "engines" in package.json.
engine-strict=true

# Workspaces install from the root; this keeps hoisting predictable so
# app/server and root tooling share one @prisma/client.
install-strategy=hoisted
